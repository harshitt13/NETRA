# Frontend environment example

# Public API base for the backend (Render/Render equivalent)
# e.g., https://your-backend.onrender.com/api
VITE_API_URL=

# Dev proxy target for Vite (used only in `vite.config.js` during `vite dev`)
# e.g., http://127.0.0.1:5001
VITE_DEV_PROXY_TARGET=http://127.0.0.1:5001

# Use mock auth (true/false); when true, the app uses a fake token
REACT_APP_USE_MOCK_AUTH=true

# Firebase (client-side) configuration
# These are public client-side keys; still avoid committing real values.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
